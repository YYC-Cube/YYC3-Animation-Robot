packages:
  - packages/*

allowBuilds:
  sharp: true
  unrs-resolver: false

# 供应链策略：minimumReleaseAge(24h) 拦截冷却期内的新发布，
# overrides 通配钉住合规旧版；各包过 24h 冷却期后可移除对应行。
overrides:
  rolldown: 1.2.9
  '@rolldown/binding-*': 1.2.9
  magic-string: 1.4.1
